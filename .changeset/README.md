# Changesets

Hi! This directory tracks changes intended for release. See
<https://github.com/changesets/changesets> for the full flow.

Quick reference:

- `pnpm changeset` — record a new changeset (interactive).
- `pnpm changeset version` — consume changesets, bump versions, update changelogs.
- `pnpm changeset publish` — publish changed packages to npm.
