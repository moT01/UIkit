---
'@freecodecamp/uikit': minor
'@freecodecamp/uikit-css': patch
---

Add `<StackedLayout>` — full-page chrome for top-nav pages. Optional
`header` slot on top, flex-grow `<main>` in the middle, optional
`footer` slot at the bottom. Slot containers only render when the
corresponding prop is provided.
