---
'@freecodecamp/uikit-docs': patch
---

Add a scoped palette gallery to `/foundations/colors`. Two tiles
render the same miniature assortment inside a `.dark-palette` and a
`.light-palette` root so readers can compare surfaces + CTAs without
toggling the whole page. Demonstrates how the palette classes scope
to any subtree, not just `<html>`.
