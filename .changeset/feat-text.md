---
'@freecodecamp/uikit': minor
'@freecodecamp/uikit-css': patch
---

Add `<Text>` primitive — polymorphic body-copy wrapper with four size
steps (`xs|sm|base|lg`), two weights, three tones, and Lato by default.
Every styling decision maps to tokens; consumers swap the rendered
element via `as`.
