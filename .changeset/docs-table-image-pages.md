---
'@freecodecamp/uikit-docs': patch
---

Add dedicated MDX pages for `<Table>` and `<Image>` — the two
storybook-only components left out of the 3C pass. Both ship with the
full template (Props, Accessibility, Tokens, Do/Don't, Related) and
link from the sidebar + component index like every other component.
