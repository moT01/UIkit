---
'@freecodecamp/uikit': minor
'@freecodecamp/uikit-css': patch
---

Add `<Fieldset>` form primitive — native fieldset + legend pair styled
with our border/spacing tokens. `legend` accepts any ReactNode; `tone`
switches between default and subtle borders; `disabled` cascades to
every contained control via the native fieldset semantics.
