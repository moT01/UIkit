// Non-React entry: exposes the icon body map and type union for any
// consumer (vanilla L2 runtime, build tooling, tests) that does not
// want React in the dependency graph.
export { icons, svgAttrs, type IconName } from './icons';
