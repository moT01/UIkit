---
import PlaygroundCard from '../components/site/PlaygroundCard.astro';
import { Code } from 'astro/components';
import { ModalDemo } from './_islands/ModalDemo';
---

<PlaygroundCard
  id='modal'
  slug='Modal'
  title='Modal'
  previewStacked
  previewPlain
  description='Blocking dialog — confirmations and short-form destructive choices.'
  source='overlays/Modal.tsx'
  status='stable'
  since='1.0.0'
  a11yPattern='https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/'
>
  <Fragment slot='preview'>
    <ModalDemo client:load />
  </Fragment>
  <Code
    slot='react'
    lang='tsx'
    code={`import { Modal, Button } from '@freecodecamp/uikit';

<Modal open={open} onClose={close} title="Reset progress?">
  <Modal.Body>
    You completed 17 of 32 steps in Responsive Web Design.
  </Modal.Body>
  <Modal.Footer>
    <Button onClick={close}>Cancel</Button>
    <Button variant="danger" onClick={reset}>Reset progress</Button>
  </Modal.Footer>
</Modal>`}
  />
  <Code
    slot='html'
    lang='html'
    code={`<div class="modal" role="dialog" aria-labelledby="m-title">
  <header class="modal__header">
    <p class="modal__title" id="m-title">Reset progress?</p>
    <button class="close-btn" aria-label="Close">×</button>
  </header>
  <div class="modal__body"><p>...</p></div>
  <footer class="modal__footer">
    <button class="btn">Cancel</button>
    <button class="btn btn--danger">Reset progress</button>
  </footer>
</div>`}
  />
</PlaygroundCard>
