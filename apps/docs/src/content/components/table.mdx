---
title: Table
eyebrow: <Table />
status: stable
since: '0.1.0'
category: data-display
summary: The semantic table primitive. Pair with `<DataTable>` when sortable columns + zebra striping are needed; reach for raw `<Table>` for simple tabular data.
a11yPattern: https://www.w3.org/TR/wai-aria-practices-1.2/examples/table/table.html
tokens:
  - --foreground-primary
  - --foreground-secondary
  - --foreground-quaternary
  - --background-secondary
order: 13
---

import PropTable from '../../components/site/PropTable.astro';
import TokenChips from '../../components/site/TokenChips.astro';
import DoDont from '../../components/site/DoDont.astro';
import RelatedComponents from '../../components/site/RelatedComponents.astro';

`<Table>` is the thin semantic wrapper over `<table>` / `<thead>` /
`<tbody>` — it applies the Command-line Chic surface styling and
exposes compound sub-components (`Table.Head`, `Table.Body`,
`Table.Row`, `Table.Cell`) so you don't restyle every row by hand.

## Props

<PropTable
  rows={[
    {
      name: 'caption',
      type: 'ReactNode',
      description:
        'Rendered inside `<caption>`; announced by screen readers before the data.'
    },
    {
      name: 'dense',
      type: 'boolean',
      default: 'false',
      description: 'Tightens row padding from 12 px to 8 px for log-style data.'
    },
    {
      name: 'Table.Cell.align',
      type: "'start' | 'center' | 'end'",
      default: "'start'",
      description: 'Text alignment inside the cell.'
    },
    {
      name: 'Table.Cell.scope',
      type: "'row' | 'col'",
      description:
        'Marks header cells — forwards to the native `scope` attribute.'
    }
  ]}
/>

## Accessibility

Renders a native `<table>`; screen readers announce row/column counts
automatically. Always include a `caption` when the table stands alone
on a page, or wrap in a `<section aria-labelledby>` when the heading
lives nearby. Use `scope="col"` or `scope="row"` on header cells so
cell-to-header associations survive linearised reading.

## Tokens consumed

<TokenChips
  tokens={[
    '--foreground-primary',
    '--foreground-secondary',
    '--foreground-quaternary',
    '--background-secondary'
  ]}
/>

## Do & Don't

<DoDont>
  <Fragment slot='do'>
    <ul>
      <li>Reach for `<DataTable>` when users need to sort, filter, or page.</li>
      <li>Write a `caption` that summarises the data in one line.</li>
      <li>Use `dense` for log/terminal-style content; keep defaults for human copy.</li>
    </ul>
  </Fragment>
  <Fragment slot='dont'>
    <ul>
      <li>Don't use tables for layout; reach for `<Grid>` or flex.</li>
      <li>Don't style a div-grid to look like a table — assistive tech won't see the relationships.</li>
      <li>Don't mix `<Table>` with `<DataTable>` on the same page; pick one.</li>
    </ul>
  </Fragment>
</DoDont>

<RelatedComponents slugs={['data-table', 'description-list', 'panel']} />
