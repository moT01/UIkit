---
title: Link
eyebrow: <Link />
status: stable
since: '0.1.0'
category: primitive
summary: The styled anchor primitive. Wraps `<a>` with the UIKit hover/focus ring and a `block` flag for full-width link rows.
a11yPattern: https://www.w3.org/WAI/ARIA/apg/patterns/link/
tokens:
  - --link-foreground
  - --link-hover-foreground
  - --focus-outline
order: 4
---

import PropTable from '../../components/site/PropTable.astro';
import TokenChips from '../../components/site/TokenChips.astro';
import DoDont from '../../components/site/DoDont.astro';
import RelatedComponents from '../../components/site/RelatedComponents.astro';

`<Link>` is the styled anchor. It forwards every native anchor
attribute and adds a `block` boolean so the anchor can own an entire
row — navigation menus, card-wide click targets.

## Props

<PropTable
  rows={[
    {
      name: 'href',
      type: 'string',
      description: 'Forwarded to the underlying `<a>`. Required.'
    },
    {
      name: 'block',
      type: 'boolean',
      default: 'false',
      description:
        'Renders as `display: block` so the hit target fills its parent row.'
    },
    {
      name: 'external',
      type: 'boolean',
      default: 'false',
      description:
        'Adds `target="_blank" rel="noreferrer noopener"` + a trailing icon.'
    },
    {
      name: 'variant',
      type: "'default' | 'subtle'",
      default: "'default'",
      description: '`subtle` mutes the colour for secondary nav contexts.'
    }
  ]}
/>

## Keyboard

| Key   | Action                                     |
| ----- | ------------------------------------------ |
| Enter | Activates the link (native browser).       |
| Tab   | Moves focus to the next focusable element. |

## Accessibility

Renders a native `<a>`, so assistive tech treats it like a link and not
a button. Use `<Button>` for actions that change state on the current
page — don't weaponise links for that purpose.

## Tokens consumed

<TokenChips
  tokens={['--link-foreground', '--link-hover-foreground', '--focus-outline']}
/>

## Do & Don't

<DoDont>
  <Fragment slot='do'>
    <ul>
      <li>Use `block` when the link fills a card or row — makes the whole area clickable.</li>
      <li>Set `external` for cross-origin links so users know the jump.</li>
      <li>Write descriptive text ("View component source") — avoid "click here".</li>
    </ul>
  </Fragment>
  <Fragment slot='dont'>
    <ul>
      <li>Don't style a `<Button>` as a link or vice versa — semantics match behaviour.</li>
      <li>Don't nest interactive children; WCAG disallows button-inside-link.</li>
      <li>Don't use `subtle` as the only signal — pair with underline on hover.</li>
    </ul>
  </Fragment>
</DoDont>

<RelatedComponents slugs={['button', 'navbar', 'sidebar']} />
