---
title: Close button
eyebrow: <CloseButton />
status: stable
since: '0.1.0'
category: primitive
summary: The small × affordance for dismissing panels, toasts, and modals. Defaults to `aria-label='Close'` so every dismiss target stays labelled.
a11yPattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/
tokens:
  - --foreground-primary
  - --foreground-muted
  - --background-secondary
order: 3
---

import PropTable from '../../components/site/PropTable.astro';
import TokenChips from '../../components/site/TokenChips.astro';
import DoDont from '../../components/site/DoDont.astro';
import RelatedComponents from '../../components/site/RelatedComponents.astro';

`<CloseButton>` wraps a native `<button>` with the shared dismiss
visual and a default `aria-label`. It's the right primitive for toast
close affordances, modal close-×, and dismissible banners — a single
component keeps every dismiss target looking the same.

## Props

<PropTable
  rows={[
    {
      name: 'onClick',
      type: '(event: MouseEvent) => void',
      description: 'Called on click or keyboard activation.'
    },
    {
      name: 'size',
      type: "'sm' | 'md'",
      default: "'md'",
      description: '32 px hit target on `md`, 24 px on `sm`.'
    },
    {
      name: '"aria-label"',
      type: 'string',
      default: "'Close'",
      description: 'Accessible name. Override when the context is ambiguous.'
    }
  ]}
/>

## Keyboard

| Key           | Action                                            |
| ------------- | ------------------------------------------------- |
| Space / Enter | Fires `onClick`.                                  |
| Escape        | If parent is `<Modal>`, dismisses the dialog too. |

## Accessibility

Native `<button>` labelled `"Close"` by default. Always pair with a
discoverable keyboard path — Escape on `<Modal>` mirrors the click.
Override `aria-label` when the surface context matters
("Close filter drawer").

## Tokens consumed

<TokenChips
  tokens={[
    '--foreground-primary',
    '--foreground-muted',
    '--background-secondary'
  ]}
/>

## Do & Don't

<DoDont>
  <Fragment slot='do'>
    <ul>
      <li>Place it in the top-right corner of the surface it dismisses.</li>
      <li>Override `aria-label` when the default "Close" is ambiguous.</li>
      <li>Keep hit target ≥ 32 px on touch surfaces.</li>
    </ul>
  </Fragment>
  <Fragment slot='dont'>
    <ul>
      <li>Don't use it for "Cancel" or "Back" — those deserve text labels.</li>
      <li>Don't shrink below 24 px — accessibility target minimum.</li>
      <li>Don't restyle the × glyph; it's part of the design contract.</li>
    </ul>
  </Fragment>
</DoDont>

<RelatedComponents slugs={['button', 'toggle-button', 'modal']} />
