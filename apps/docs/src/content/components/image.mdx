---
title: Image
eyebrow: <Image />
status: stable
since: '0.1.0'
category: primitive
summary: A responsive `<img>` primitive with a required `alt` contract. Pair with a `<figure>` + `<figcaption>` compound when the image needs a caption.
a11yPattern: https://www.w3.org/WAI/tutorials/images/
tokens:
  - --background-secondary
  - --foreground-quaternary
order: 6
---

import PropTable from '../../components/site/PropTable.astro';
import TokenChips from '../../components/site/TokenChips.astro';
import DoDont from '../../components/site/DoDont.astro';
import RelatedComponents from '../../components/site/RelatedComponents.astro';

`<Image>` is the responsive image primitive. It forwards every native
`<img>` attribute and enforces the `alt` contract at the type level —
there's no way to render the component without declaring one, which
keeps the accessibility floor high.

## Props

<PropTable
  rows={[
    {
      name: 'src',
      type: 'string',
      description: 'Forwarded to the native `<img>`. Required.'
    },
    {
      name: 'alt',
      type: 'string',
      description:
        'Alternate text. Pass `""` for purely decorative images; anything else must describe the content.'
    },
    {
      name: 'caption',
      type: 'ReactNode',
      description: 'When set, wraps the image in `<figure>` + `<figcaption>`.'
    },
    {
      name: 'aspect',
      type: "'square' | 'landscape' | 'portrait'",
      description: 'Locks `aspect-ratio` to avoid layout shift while loading.'
    },
    {
      name: 'fit',
      type: "'cover' | 'contain'",
      default: "'cover'",
      description: 'Maps to `object-fit`.'
    }
  ]}
/>

## Accessibility

Renders a native `<img>`; screen readers read `alt` in place of the
image. Empty `alt=""` signals purely decorative — the image is skipped
in the reading order. Use `caption` when the image is meaningful but
the surrounding prose already describes it; captions are
programmatically associated via `<figcaption>`.

## Tokens consumed

<TokenChips tokens={['--background-secondary', '--foreground-quaternary']} />

## Do & Don't

<DoDont>
  <Fragment slot='do'>
    <ul>
      <li>Always lock `aspect` so the layout doesn't jump during load.</li>
      <li>Use `alt=""` for pure decoration; anything else describes content.</li>
      <li>Serve responsive sources via `srcSet` when the image is hero-sized.</li>
    </ul>
  </Fragment>
  <Fragment slot='dont'>
    <ul>
      <li>Don't write "image of…" in `alt` — screen readers already announce the role.</li>
      <li>Don't embed text inside a bitmap image; use real text next to it.</li>
      <li>Don't use `<Image>` for icons — reach for `@freecodecamp/uikit-icons`.</li>
    </ul>
  </Fragment>
</DoDont>

<RelatedComponents slugs={['avatar', 'card', 'panel']} />
