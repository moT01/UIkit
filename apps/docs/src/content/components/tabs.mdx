---
title: Tabs
eyebrow: <Tabs />
status: stable
since: '0.1.0'
category: navigation
summary: A keyboard-accessible tab panel with `<Tab eventKey>` children and an optional controlled `activeKey`. Arrow keys move focus; Enter activates.
a11yPattern: https://www.w3.org/WAI/ARIA/apg/patterns/tabs/
tokens:
  - --foreground-primary
  - --foreground-secondary
  - --foreground-quaternary
  - --background-tertiary
order: 11
---

import PropTable from '../../components/site/PropTable.astro';
import TokenChips from '../../components/site/TokenChips.astro';
import DoDont from '../../components/site/DoDont.astro';
import RelatedComponents from '../../components/site/RelatedComponents.astro';

`<Tabs>` + `<Tab>` render a proper `role="tablist"` / `role="tab"` /
`role="tabpanel"` triad. Internally composed on `@ark-ui/react/tabs`,
so Arrow keys move focus between triggers, Home/End jump to the ends,
and Enter / Space activate. Works controlled via `activeKey` or
uncontrolled via `defaultActiveKey`.

## Props

<PropTable
  rows={[
    {
      name: 'activeKey',
      type: 'string',
      description:
        'Controlled selection — the `eventKey` of the active `<Tab>`.'
    },
    {
      name: 'defaultActiveKey',
      type: 'string',
      description:
        'Uncontrolled initial selection. Falls back to the first Tab.'
    },
    {
      name: 'onSelect',
      type: '(key: string) => void',
      description: 'Called whenever the active key changes.'
    },
    {
      name: 'Tab.eventKey',
      type: 'string',
      description: 'Unique identifier for the tab. Pairs trigger and panel.'
    },
    {
      name: 'Tab.title',
      type: 'ReactNode',
      description: 'Trigger label rendered in the tablist.'
    }
  ]}
/>

## Keyboard

| Key               | Action                            |
| ----------------- | --------------------------------- |
| ArrowLeft / Right | Cycle triggers horizontally.      |
| ArrowUp / Down    | Cycle triggers when vertical.     |
| Home / End        | Jump to the first / last trigger. |
| Enter / Space     | Activate the focused trigger.     |
| Tab               | Moves focus out of the tablist.   |

## Accessibility

Full APG [tabs](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/)
compliance. Triggers expose `aria-selected` and `data-selected=""`;
panels expose `role="tabpanel"` + `data-state="open|closed"`. Inactive
panels receive the `hidden` attribute so screen readers skip them.

## Tokens consumed

<TokenChips
  tokens={[
    '--foreground-primary',
    '--foreground-secondary',
    '--foreground-quaternary',
    '--background-tertiary'
  ]}
/>

## Do & Don't

<DoDont>
  <Fragment slot='do'>
    <ul>
      <li>Use Tabs for parallel views of the same content (Code / Preview / Notes).</li>
      <li>Keep 2–5 tabs; beyond that, consider a sidebar list.</li>
      <li>Write short trigger labels — one or two words.</li>
    </ul>
  </Fragment>
  <Fragment slot='dont'>
    <ul>
      <li>Don't hide critical actions behind a tab the user might miss.</li>
      <li>Don't use Tabs for a linear wizard flow — reach for `<FormStepper>`.</li>
      <li>Don't nest Tabs inside Tabs; the keyboard contract collapses.</li>
    </ul>
  </Fragment>
</DoDont>

<RelatedComponents slugs={['navbar', 'sidebar', 'pagination']} />
