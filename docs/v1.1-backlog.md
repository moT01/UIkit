# v1.1 backlog

Captured items deferred from v1.0.0 GA. Each will be filed as a GitHub Issue when the repo flips public; this doc is the canonical interim record.

Format: `ID — title (priority) — disposition`. Cross-references use the original Wave audit IDs (`B*`, `F*`, `C*`) so links to Wave 8/9 SKILL-EXTRACT logs survive.

## Open bugs (carry to v1.0 patch line)

| ID            | Title                                                                              | Severity | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------- | ---------------------------------------------------------------------------------- | -------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B4**        | Foundations band + type-scale grids do not share column rhythm                     | P3       | open   | CSS-only fix once located. Deferred at GA cut.                                                                                                                                                                                                                                                                                                                                                                   |
| **B5**        | Hero CTA cluster + install snippet padding mismatch                                | P4       | open   | Visual polish. Deferred at GA cut.                                                                                                                                                                                                                                                                                                                                                                               |
| **UIkit-aq0** | Flaky `[desktop] PlaygroundCard modal` Playwright snapshot — sub-pixel font jitter | P2       | open   | `apps/docs/tests/visual/playground-card.spec.ts:53` carries `test.skip()` for `project=desktop && slug=modal`. Other slugs + `[desktop-light]` modal pass. Fix options: (a) replace screenshot with DOM-shape behavioural assertion, (b) mask trigger-button glyph region via `mask: [card.locator('button')]`, (c) pin chromium font-rendering flag. Acceptance: 10/10 consecutive green without `test.skip()`. |

## Resolved during v1.0 polish review (no action needed)

| ID     | Title                                                                | Resolution                                                                                                                                                                                                                                                                   |
| ------ | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B1** | Landing-page sidebar duplication regression                          | NOT-REPRO. `AppSidebar.tsx:19-61` renders single `nav.map(section => ...).map(item => ...)`. `nav.ts` is flat with no doubled entries. Stale screenshot from pre-Wave-9 sidebar rebuild. Re-file if observed live.                                                           |
| **B7** | Form-component preview renders as `<details>`                        | NOT-REPRO. `PlaygroundCard.astro:181-187` renders preview in `<div class='showcase__code'>`, not `<details>`. The `<details class='showcase__anatomy'>` at line 191 is the intentional Anatomy section (collapsed by default per W9-B15). Original report conflated the two. |
| **B3** | `/` "Pick the flavour" third card label reads as Tailwind classnames | WONT-FIX (by-design). `index.astro:118-155` surfaces band shows three flavours, each card renders its DSL: card 1 React JSX, card 2 HTML BEM, card 3 Tailwind utility classes. All three labels are the intentional API surface marketing.                                   |

## v1.1 features (deferred from Wave 7 + 8 + 9)

| ID      | Title                                               | Origin           | Notes                                                                                    |
| ------- | --------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------- |
| **F1**  | Per-component routes `/components/<slug>`           | W8-4             | IA change too large for v1.0 GA; revisit after search + PropTable stabilise in the wild. |
| **F2**  | Lighthouse perf gate in CI                          | W8-8             | Not GA-blocking; first-paint < 200 KB budget already documented.                         |
| **F3**  | Mobile + tablet light-palette parity                | Wave 8 Q4        | Wave 8 ships `desktop-light` only.                                                       |
| **F4**  | `<Breadcrumb responsive>` collapse-to-ellipsis      | Wave 8 Q3        | Ship if community asks.                                                                  |
| **F5**  | `react-docgen-typescript` upstream fix for generics | styleguidist#203 | Watch upstream; once landed, drop the stub envelope arm in `props.json` generator.       |
| **F6**  | `/components/<slug>/api.md` agent endpoint          | depends on F1    | Expose per-component `props.json` as `.md` for LLM consumers.                            |
| **F7**  | Search index analytics                              | Wave 8 Q2        | `?fuse_debug=1` opt-in score logging post-launch.                                        |
| **F8a** | Storybook revival                                   | Wave 7 follow-up | Deferred.                                                                                |
| **F8b** | i18n scaffold                                       | Wave 7 follow-up | Deferred.                                                                                |
| **F8c** | v2 component-level breaking changes                 | Wave 7 follow-up | Deferred.                                                                                |

## Component / UX debt (carried since pre-Wave-7)

| Item                                                                                  | Notes                                                                                                                                       |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Dropdown / Listbox / Combobox / CommandPalette swap to `@ark-ui/react` or `@zag-js/*` | Hand-rolled today. Breaking — ark forces `createListCollection` + `value`-required items. RFC pending.                                      |
| `<Dropdown>` keyboard-nav (arrow keys + typeahead)                                    | Missing; post-1.0.                                                                                                                          |
| Per-card Tailwind tabs revival                                                        | Cut entirely in Wave 8 (W8-11). `@freecodecamp/uikit-tailwind` package + `/guides/tailwind` survive. Re-evaluate community demand post-1.0. |

## CI hardening backlog (deferred from v1.0 polish)

| Item                            | Notes                                                                                                                                                                                                                                                      |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CI: Codecov upload**          | `re-test.yml` runs `pnpm test:coverage` + uploads coverage artefact. Codecov action upload step not added — needs (a) verified `codecov/codecov-action@v5` SHA, (b) `CODECOV_TOKEN` repo secret. Add as second job in `re-test.yml` once token configured. |
| **CI: bundle-size budget gate** | W8-8 carry-over. Add `size-limit` package + per-artefact config (styles.min.css < 50 KB gz, uikit.global.js < 100 KB gz, sprite.svg < 30 KB). Wire `andresz1/size-limit-action` for PR comments.                                                           |
| **CI: actionlint as a job**     | Currently invoked locally. Add a `re-lint.yml` step running `rhysd/actionlint` to fail-fast on workflow drift.                                                                                                                                             |
| **CI: changeset-bot**           | When repo goes public, add `changesets/action` to auto-PR version bumps. Today version bump is manual via `pnpm changeset version` per `CONTRIBUTING.md` §Release checklist.                                                                               |

## Repo-going-public follow-ups

| Item                         | Notes                                                                                                                                                                                                                 |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Add `.github/CODEOWNERS`** | Skipped at v1.0 polish (repo private). When public + onboarding contributors, add cascading rules: global `* @freeCodeCamp/dev-team`, per-package overrides, `.github/workflows/** @freeCodeCamp/ops`.                |
| **Issue templates**          | fCC contribute repo defers all bug-reporting to forum + `contribute.freecodecamp.org`. Mirror that when public, or add `.github/ISSUE_TEMPLATE/bug_report.yml` + `feature_request.yml` if signal-to-noise demands it. |
| **PR template**              | Same trade-off as above. Add `.github/PULL_REQUEST_TEMPLATE.md` with changeset reminder + visual-diff checklist.                                                                                                      |
| **`FUNDING.yml`**            | Inherit from fCC org config if available; otherwise manual entry.                                                                                                                                                     |
| **First npm publish**        | `pnpm changeset publish` with org-scoped npm token. Manual one-time.                                                                                                                                                  |
| **GitHub release notes**     | Derive from `packages/*/CHANGELOG.md` generated by `pnpm changeset version`. Optional — release.yml currently does not create a GitHub release.                                                                       |

## Field-notes drift (acceptable, archive at next sprint)

| Item                                                 | Notes                                                                           |
| ---------------------------------------------------- | ------------------------------------------------------------------------------- |
| `.scratchpad/SKILL-EXTRACT.txt`                      | Wave 9 retrospective. Keep as historical record.                                |
| `.scratchpad/plans/this-branch-has-a-vivid-shell.md` | Squash plan. Already executed; mark "DONE 2026-04-26" header line if revisited. |
